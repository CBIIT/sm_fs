import { FormatNcabDatePipe } from './format-ncab-date.pipe';

describe('FormatNcabDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatNcabDatePipe();
    expect(pipe).toBeTruthy();
  });
});
